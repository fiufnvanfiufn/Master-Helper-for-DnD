sf::Texture dragonButtonTexture;
  if (!dragonButtonTexture.loadFromFile(
          "assets/pictures/buttons/DragonButton.png")) {
    return -1;
  }

  sf::Sprite buttonSprite;
  buttonSprite.setTexture(dragonButtonTexture);
  buttonSprite.setPosition(280, 200);

  buttonSprite.setScale(0.2f, 0.2f);
  sf::Vector2f pos = buttonSprite.getPosition();
  sf::FloatRect clickableArea(pos.x + 20, pos.y + 80, 180, 40);